commands:
  cron_install: 
    command: if [[ "${RUN_CRONS}" == "true" ]] ; then echo "$(date): Crons will be run" >> /var/log/crons_install.log ; else echo "$(date): Crons will not be run" >> /var/log/crons_install.log ; fi
    # test: conditions for command 
    ignoreErrors: true

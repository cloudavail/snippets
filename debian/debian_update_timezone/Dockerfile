FROM debian:buster
# to expirement with changing the timezone, you can use
# ln -sf /usr/share/zoneinfo/America/Los_Angeles /etc/localtime
RUN ln -sf /usr/share/zoneinfo/Etc/UTC /etc/localtime
RUN echo "Etc/UTC" > /etc/timezone

